[START]
NAME:
COST:
TYPE:
.........
[END]












        enum ItemType
        {
            NONE,
            QUEST,
            CONSUMABLE,
            WEAPON,
            ARMOR,
        };

        enum EquipablePosition
        {
            NONE,
            WEAPON,
            HEAD,
            CHEST,
            ARMS,
            LEGS,
            FINGER,
            NECK
        };

        enum DamageType
        {
            NONE,
            BLUNT,
            PIERCE,
            MAGIC,
            EXPLOSIVE
        };

        // Properties all items must 
        string name = "";
        int cost = -1;
        ItemType itemType = ItemType.NONE;

        // Weapon/armor
        EquipablePosition equipablePosition = EquipablePosition.NONE;
        double armor = -1;
        double strengthModifier = -1;
        double dexterityModifier = -1;
        double intelligenceModifier = -1;
        double absorb = -1;
        DamageType damageType = DamageType.NONE;

        // Potions
        bool consumable = false;
        double hpModifier = -1;
        double mpModifier = -1;
        double damageModifier = -1;
        double armorModifier = -1;

        // Quest Item
        int questID = -1;